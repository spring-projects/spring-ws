name: "Run CodeQL Analysis"
on:
  push:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '45 2 * * 1'
permissions: read-all
jobs:
  run-analysis:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: spring-io/github-actions/.github/workflows/codeql-analysis.yml@035e701d99b48da41f409014e1bfd937425c4477
